# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
*.spec

# ── 虚拟环境（uv 管理）──
.venv/

# ── 打包产物 ──
dist*/
build*/

# ── 环境变量（含密钥 / 个人配置，勿提交）──
.env

# ── 测试 / 调试临时产物 ──
test_grab.png
test_*.png

# ── 录屏输出（运行时生成在 ~/Pictures/jianying）──
*.mp4
*.avi

# ── 项目工作记忆（个人开发日志 / 技能，勿提交）──
.workbuddy/

# ── 编辑器 / 系统 ──
.idea/
.vscode/
.DS_Store
